import React from 'react';
import MapPage from './MapPage';

// 지도 페이지에 필요한 데이터를 관리하는 컨테이너
const MapContainer = () => {
  return (
    <div>
      <MapPage />
    </div>
  );
};

export default MapContainer;
